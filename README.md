# README

### Quick Start
```bash
# clone project
git clone git@github.com:Novarty/events.git
cd events

# install gems
bundle install

# configure DB
rails db:setup

# run server on 3000 port
rails s
```
